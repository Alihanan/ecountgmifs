% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criterion_compile.R
\name{ecountgmifs.compile.criterion}
\alias{ecountgmifs.compile.criterion}
\title{Compile a custom C++ selection criterion from code}
\usage{
ecountgmifs.compile.criterion(code, symbol, name = symbol, use.cache = TRUE)
}
\arguments{
\item{code}{character. C++ source code defining the criterion function.}

\item{symbol}{character. Name of the C symbol to look up after compilation.}

\item{name}{character. Friendly name stored in the returned criterion object.}

\item{use.cache}{logical. If \code{TRUE}, reuse a previously compiled
criterion with the same \code{code} and \code{symbol}.}
}
\value{
An object of class \code{"ecountgmifs.criterion"}.
}
\description{
Compiles user-supplied C++ code defining a criterion function and returns a
criterion object that can be passed to ecountgmifs selection routines.

Compiled criteria are cached by default. Repeated calls with the same
\code{code} and \code{symbol} return the cached criterion object rather than
recompiling the C++ source.

The C++ code should define a C-compatible function with signature
\preformatted{
extern "C" double criterion_name(const Score* s);
}

The C++ code should include the public score header:
\preformatted{
#include <Rcpp.h>
// [[Rcpp::depends(ecountgmifs)]]

#include <ecountgmifs/score.h>
}
}
