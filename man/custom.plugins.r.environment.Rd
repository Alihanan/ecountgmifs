% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plugin_documentation.R
\name{custom.plugins.r.environment}
\alias{custom.plugins.r.environment}
\title{R callback plugins with explicit environment data}
\description{
The package-built generic children created by \code{\link[=r.link]{r.link()}}, \code{\link[=r.family]{r.family()}},
\code{\link[=r.family.link]{r.family.link()}}, and \code{\link[=r.criterion]{r.criterion()}} retain one \code{Rcpp::Environment}. The
exact same environment is passed as the final argument on every callback.
This style makes fixed matrices, thresholds, hyperparameters, counters, and
caches explicit and inspectable with \code{\link[=r.plugin.environment]{r.plugin.environment()}}.
}
\section{Complete R examples}{

}

\examples{
capped.log.environment.code <- '
workspace <- r.environment(mu.min = 1e-12, mu.max = 1e12)
link <- r.link(
  name = "capped.log",
  environment = workspace,
  inverse = function(eta, link.parameters, environment) {
    pmin(pmax(exp(eta), environment$mu.min), environment$mu.max)
  },
  grad = function(eta, link.parameters, environment) {
    list(
      d_mu_d_eta = exp(eta),
      d_mu_d_link_parameters = matrix(numeric(), length(eta), 0L)
    )
  }
)
'

ebic.environment.code <- '
workspace <- r.environment(gamma = 0.5)
criterion <- r.criterion(
  name = "EBIC.environment",
  environment = workspace,
  evaluate = function(input, control, state, environment) {
    nnz <- sum(state$beta != 0)
    2 * state$negloglik + log(input$n) * nnz +
      2 * environment$gamma * lchoose(input$p, nnz)
  }
)
'

link <- capped.log.link.r.environment()
criterion <- EBIC.r.environment(gamma = 0.5)

}
\seealso{
\link{custom.plugins}, \link{custom.plugins.r.closure}, \code{\link[=r.environment]{r.environment()}}
}
